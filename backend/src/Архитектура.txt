backend/
├── src/
│   ├── index.js          // Точка входа в приложение
│   ├── server.js         // Файл для создания и настройки Express-приложения (опционально, но полезно для тестирования)
│   ├── routes/           // Здесь будут файлы с маршрутами (routes)
│   │   └── taskRoutes.js // Маршруты для задач
│   │   └── authRoutes.js // Маршруты для аутентификации
│   ├── controllers/      // Здесь будут файлы с обработчиками запросов (controllers)
│   │   └── taskController.js // Логика для обработки запросов к задачам
│   │   └── authController.js // Логика для обработки запросов аутентификации
│   ├── middleware/       // Здесь будут промежуточные обработчики (middleware)
│   │   └── authMiddleware.js // Middleware для проверки JWT
│   │   └── errorHandler.js    // Централизованный обработчик ошибок
│   └── prisma/           // (уже существует после `prisma init`)
│       └── client.js     // Файл для экспорта экземпляра Prisma Client (опционально, но удобно)
├── prisma/               // (уже существует)
└── ...